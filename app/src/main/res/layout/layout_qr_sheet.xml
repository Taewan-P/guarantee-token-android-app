<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="400dp"
    android:padding="16dp"
    android:clipToPadding="false"
    android:background="@drawable/shape_send_sheet">

    <ImageView
        android:id="@+id/dismiss_address_btn"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:src="@drawable/ic_xmark"/>


    <TextView
        android:id="@+id/my_address_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="My Address"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="@color/white" />

    <ImageView
        android:id="@+id/my_qr_img"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_marginTop="50dp"
        android:layout_marginBottom="25dp"
        android:layout_below="@id/my_address_title"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="25dp"
        android:layout_below="@id/my_qr_img"
        android:text="Scan this QR code to receive token."
        android:textColor="@color/white"
        android:textSize="17sp"
        android:textAlignment="center"
        android:textStyle="bold" />
</RelativeLayout>